<%= form_for(@sex) do |f| %>
  <% if @sex.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sex.errors.count, "error") %> prohibited this sex from being saved:</h2>

      <ul>
      <% @sex.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sex %><br />
    <%= f.text_field :sex %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
